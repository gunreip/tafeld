diff --git a/app/Console/Commands/ProjectAuditMain.php b/app/Console/Commands/ProjectAuditMain.php
--- a/app/Console/Commands/ProjectAuditMain.php
+++ b/app/Console/Commands/ProjectAuditMain.php
@@ -87,7 +87,14 @@
         foreach ($files as $absPath) {
             $rel = ltrim(str_replace($root, '', $absPath), DIRECTORY_SEPARATOR);
             $dir = trim(dirname($rel), '/\\');
             if ($dir === '.') $dir = '';
-            $byDir[$dir] ??= [];
-            $byDir[$dir][] = basename($rel);
-        }
+            $byDir[$dir] ??= [];
+            $byDir[$dir][] = [
+                'name' => basename($rel),
+                'path' => $absPath
+            ];
+        }
@@ -139,7 +146,15 @@
-            foreach ($files as $file) {
-                // Link relativ zu .logs
-                $href = ($dir === '' ? './' : './' . str_replace('\\', '/', $dir) . '/') . $file;
-                $link = '<span class="project-audit-main-link"><a href="' . htmlspecialchars($href, ENT_QUOTES) . '">' . htmlspecialchars($file, ENT_QUOTES) . '</a></span>';
-                $desc = '<span class="audit-desc">' . $commentRaw . '</span>';
-                $body .= '<li>' . $link . ' ' . $desc . '</li>';
-            }
+            foreach ($files as $entry) {
+                $file = $entry['name'];
+                $absPath = $entry['path'];
+                $href = ($dir === '' ? './' : './' . str_replace('\\', '/', $dir) . '/') . $file;
+                $mtime = @filemtime($absPath);
+                $stamp = $mtime ? date('Y-m-d H:i:s', $mtime) : 'n/a';
+                $link = '<span class="project-audit-main-link"><a href="' . htmlspecialchars($href, ENT_QUOTES) . '">' . htmlspecialchars($file, ENT_QUOTES) . '</a></span>';
+                $desc = '<span class="audit-desc">' . $commentRaw . '</span>';
+                $ts   = '<span class="audit-timestamp">' . htmlspecialchars($stamp, ENT_QUOTES) . '</span>';
+                $body .= '<li>' . $link . ' ' . $desc . ' ' . $ts . '</li>';
+            }
diff --git a/.logs/audits-main.css b/.logs/audits-main.css
--- a/.logs/audits-main.css
+++ b/.logs/audits-main.css
@@ -20,7 +20,15 @@
-li { padding: 4px 0; border-bottom: 1px dashed var(--border); }
+li {
+  padding: 4px 0;
+  border-bottom: 1px dashed var(--border);
+  display: grid;
+  grid-template-columns: max-content 1fr auto;
+  column-gap: 8px;
+  align-items: baseline;
+}
@@ -40,6 +48,7 @@
 .audit-desc { margin-left: 8px; color:#9aa; }
+.audit-timestamp { color:#777; font-size:0.9em; font-style:italic; white-space:nowrap; }
